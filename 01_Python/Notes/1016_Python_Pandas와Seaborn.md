# Python Pandas와 Seaborn

## 🎯 목표
1. 데이터 사이언스
2. Pandas
3. 특성(Feature)
4. Seaborn

---

## 1. 데이터 사이언스
### 1-1. 데이터 분석이란? (Data Analysis)
- 데이터를 수집, 정제, 가공하여 의미 있는 정보(Insight)를 찾아내는 과정이다.
- "무슨 일이 일어났는가?"와 "왜 일어났는가?"에 집중

### 1-2. 데이터 사이언스란? (Data Science)
- 데이터 분석을 포함하여 수학, 통계학, 컴퓨터 프로그래밍, 머신러닝 등을 융합한 학문 
- 데이터를 통해 패턴을 발견하고, 미래를 예측하거나 의사결정을 자동화하는 시스템을 구축한다.

### 1-3. 데이터 사이언스를 배워야 하는 이유
- 현대의 비즈니스는 '감'이 아닌 '데이터'에 기반한다.
- 데이터는 "21세기의 석유"
- 방대한 양의 데이터 속에서 규칙성을 찾아내고, 이를 통해 비즈니스의 효율성을 극대화하거나 새로운 가치를 창출하는 능력은 현대 사회의 필수 역량이다.

### 1-4. 데이터 엔지니어링 vs 데이터 사이언스
|구분|데이터 엔지니어링 | 데이터 사이언스|
|--|-------------|-------------|
|초점| 데이터 수집, 저장, 처리|데이터 분석, 예측|
|주요도구|SQL, Spark, ETL | Pandas, NumPy, ML|
|목표|데이터 파이프라인(인프라) 구축| 모델링, 인사이트 도출|

## 2. Pandas
### 2-1. Pandas린?
- 파이썬에서 구조화된 데이터를 빠르고 쉽고 표현력 있게 다룰 수 있도록 설계된 라이브러리
- 통계, 필터링, 그룹화, 시각화까지 한 번에 가능하다.

### 2-2. Pandas를 사용하는 이유와 장점
- 엑셀과 같은 표 형식의 데이터를 다루기에 최적화되어 있다.
- 대용량 데이터의 로딩과 처리가 매우 빠르다.
- SQL, JSON, CSV 등 다양한 형식의 데이터를 자유롭게 읽고 쓸 수 있다.
- 데이터 정제, 결측치 처리, 통계 분석 기능이 매우 강력하다.

### 2-3. Pandas 데이터프레임(DataFrame)의 구조
- Index: 행(Row)을 식별하는 고유의 번호나 이름 (세로 축)
- Column: 열(Feature)의 이름 (가로 축)
- Values: 실제 데이터 값

### 2-4. Pandas 데이터프레임(DataFrame) 생성 및 기초 조작
#### 2-4-1. 데이터프레임 생성 (Dictionary 활용)
```Python
import pandas as pd

data = {
    'Name': ['Alice', 'Brian', 'Chris', 'Daisy'],
    'Age': [25, 30, 35, 40],
    'Score': [85, 90, 95, 100]
}
df = pd.DataFrame(data)
```
#### 2-4-2. 인덱스 조작
```Python
df.index = ['a', 'b', 'c', 'd'] # 인덱스 직접 변경
df.reset_index(drop=True) # 인덱스 초기화
```

#### 2-4-3. 컬럼 조작
```Python
# 컬럼 선택
df['Name']          # 컬럼 하나 선택 (Series)
df[['Name', 'Age']] # 여러 컬럼 선택 (DataFrame)

# 새로운 컬럼 추가
df['Gender'] = ['F', 'M', 'M', 'F']

# 컬럼 삭제 (axis=1은 열)
df.drop('Gender', axis=1, inplace=True)
# inplace=True : DataFrame을 직접 수정하고 새로운 DataFrame을 반환하지 않는 것
```

#### 2-4-4. 행 조작
```Python
# 행 선택
df.loc['a'] # 인덱스 이름 기반 행 선택
df.iloc[0]  # 정수 기반 기반 행 선택

# 행 조건 필터링
# True/False 값으로 조건 필터링
df[df['Score'] >= 90] # 점수가 90점 이상인 행 추출

# 여러 조건 결합 (&: AND, |: OR) 괄호 필수!
df[(df['Score'] >= 90) & (df['Age'] < 35)] # 점수가 90점 이상이고, 나이가 35살 미만인 행 추출
df[(df['Score'] < 90) | (df['Name'] == 'Alice')] # 점수가 90점 미만이거나, 이름이 Alice인 행 추출

# 값 수정
df['Age'] = [26, 31, 36, 41] # 직접 대입해서 값 변경
df.loc[df['Name'] == 'Chris', 'Score'] = 80 # 이름이 Chris인 행의 Score 값을 80으로 변경

# 행 삭제 (axis=0은 행)
df.drop(0, axis=0)
# 기본값 inplace=False 상태에서는 삭제 후 변수에 할당해야 새로운 DataFrame이 반환된다.

```

#### 2-4-5. 정렬
```Python
df.sort_values(by='Age', ascending=False) # 나이(데이터 값) 기준 내림차순

df.sort_index(axis=1) # 컬럼 이름 기준 오름차순 (Age, Name, Score)
df.sort_index(axis=0, ascending=False) # 행 인덱스 기준 내림차순 (3, 2, 1, 0)
```

## 3. 특성(Feature)
### 3-1. 특성(Feature)이란?
- 데이터의 개별 속성
- 데이터프레임에서 **하나의 컬럼**이 **하나의 Feature**

### 3-2. 머신러닝에서 Feature의 역할
- 머신러닝 모델이 학습하는 변수
- 집 값을 예측할 떄 '방의 개수', '평수', '위치' 등이 Feature가 된다.
- 유용한 Feature가 많을 수록 예측 정확도가 올라간다. (하지만 과적합 주의)
- 어떤 Feature가 중요한지를 판단하는 것도 분석의 핵심

## 4. Seaborn
### 4-1. Seaborn이란?
- Matplotlib을 기반으로 만들어진 파이썬 데이터 시각화 라이브러리
- 통계 시각화에 최적화되어 있다.
- 유명한 샘플 데이터도 포함되어 있다. (titanic, iris 등)

### 4-2. Seaborn으로 데이터 불러오기
```Python
import seaborn as sns

df = sns.load_dataset('titanic')
```

### 4-3. 데이터 상태 확인하기

```Python
# 기본값: 별도의 숫자를 입력하지 않으면 상위 5개 행 보여준다.
# titanic.head(n) : 상위 n개 행을 보여준다.
df.head()

# 전체 행, 열의 개수, 데이터 타입, 결측치가 아닌 값의 개수 확인
df.info()

# 수치형 데이터의 통계 확인(평균, 표준편차, 최솟값, 사분위수, 최댓값)
df.describe()
df.describe(include='all')    # 범주형 데이터까지 모두 표시
df.describe(include='object') # 문자형 데이터만 표시
```

### 4-4. 결측치란?
- 데이터 값이 비어있는 상태(NaN, Null)

### 4-5. 결측치 처리 방법
```Python
# 결측치 확인
df.isnull().sum()

# 결측치 채우기
df['age'].fillna(df['age'].mean(), inplace=True)    # 평균값으로 결측치 채우기
df['age'].fillna(df['age'].median(), inplace=True)  # 중앙값으로 결측치 채우기
df['age'].fillna(df['age'].mode()[0], inplace=True) # 최빈값으로 결측치 채우기
# 최빈값은 Series로 반환 하기 때문에 [0]으로 인덱싱한 값을 넣어줘야함!

# 결측치 제거
df.['deck'].dropna(inplace=True)
```