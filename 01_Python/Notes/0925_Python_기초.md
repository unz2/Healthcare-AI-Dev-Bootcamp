# Python 기초

## 🎯 목표
1. 프로그래밍 핵심 개념
2. Python과 Python의 특징
3. 아나콘다 가상환경 설치 (Mac)
4. Python 입력과 출력
5. Python 변수
6. Python 함수
---
## 1. 프로그래밍(Programming)이란?
- 컴퓨터에게 우리가 원하는 작업을 수행하도록 지시를 내리는 행위  
- 프로그래밍은 컴퓨터가 이해할 수 있는 언어(프로그래밍 언어)를 사용하여 특정 순서와 논리에 따라 명령을 작성하는 과정
---
## 1-1. 컴파일러(Compiler) vs 인터프리터(Interpreter)
- 컴파일러와 인터프리터는 모두 고급언어로 작성된 소스 코드를 컴퓨터가 이해할 수 있는 **기계어로 번역하는 역할**을 수행한다.

| 특징    | 컴파일러 (Compiler) | 인터프리터 (Interpreter) |
| -------| ----------------- | --------------------- |
| 번역 시점 | 프로그램을 전체 실행하기 전에 한 번에 번역| 프로그램을 실행하는 동안 한 번에 한 줄씩 번역하고 실행 |
| 출력 파일 | 번역 후 독립적인 실행 파일 생성| 별도의 실행 파일을 생성하지 않고 즉시 실행|
| 소스 코드 |실행 시 원본 소스코드가 필요하지 않음 |실행시 원본 소스코드가 매번 필요 |
| 실행 속도 |번역 과정이 길지만, 한 번 컴파일된 후에는 실행속도가 빠름 | 번역 과정은 짧지만, 실행할 때마다 번역하므로 실행속도가 느림|
| 오류 발견 |컴파일 단계에서 전체 오류를 한 번에 보고 |실행 중 해당 줄에 도달했을 때 줄별 오류 보고 |
| 대표 언어 | C, C++|Python, JavaScript |


---

## 2. 파이썬(Python)이란?
1991년 귀도 반 로섬에 의해 개발된 고급(High-level) 프로그래밍 언어
- **높은 가독성 및 간결성** : 문법이 직관적이고 간결하여 가독성이 높음, 들여쓰기로 코드블록을 구분함
- **인터프리터 언어** : 컴파일 과정이 없어 코드를 수정하고 결과를 바로 확인하기 쉬움
- **동적 타이핑** : 변수 선언 시 자료형을 미리 지정할 필요 없음
- **풍부한 라이브러리 생태계** : AI, 데이터, 웹, 자동화 라이브러리 생태계가 풍부하다
---

## 3. 아나콘다(Anaconda)란?
아나콘다는 파이썬과 R 언어를 기반으로 하는 데이터 과학 및 머신러닝을 위한 통합 배포판이자 패키지 관리 시스템
- **통합환경** : 파이썬 자체뿐만 아니라 데이터 분석에 필요한 NumPy, Pandas, Scikit-learn, Jupyter Notebook 등의 핵심 라이브러리 및 도구를 미리 설치된 상태로 제공한다.
- **패키지 관리** : `conda` 패키지 관리자를 통해 쉽게 라이브러리 설치, 업데이트, 제거가 가능하다.
- **가상 환경 관리** : 가상 환경을 쉽고 안정적으로 생성 및 관리할 수 있다.

---
## 3-1. 가상 환경을 사용해야 하는 이유
각각의 프로젝트에서 사용하는 라이브러리가 서로 충돌하지 않도록 격리된 공간을 제공한다.
> 프로젝트 A는 NumPy v1.0이 필요하고, 프로젝트 B에서는 v2.0이 필요한 경우 (버전 충돌 방지)

---
## 3-2. 아나콘다 설치
- 아나콘다 공식 사이트에서 OS에 맞는 설치 파일 다운로드
https://www.anaconda.com/download
- 기본 옵션으로 설치 (PATH 자동 등록)

---
## 3-3. 가상환경 만들기 (conda)
```
# 새 환경 만들기
conda create -n myenv python=3.12

# 환경 활성화하기
 conda activate myenv

# 환경 목록 보기
conda env list

# 환경 삭제 하기
conda remove -n myenv --all
```

---
## 3-4. Jupyter Notebook 실행 (Mac)
Mac 환경에서는 Jupyter Notebook 설치 필요  
Window 환경은 Anaconda 설치 시 자동으로 설치됨

`pip install jupyterlab`

---
## 4. Python 입력과 출력
**입력(Input)**
- `input()` 함수 사용
- 키보드로 입력을 받아 **문자열**로 저장되기 떄문에 숫자가 필요한 경우 형 변환 필요 (`int()`, `float()`)
```Python
name = input('이름을 입력하세요: ')
age = int(input('나이를 입력하세요: '))
print('안녕하세요,', name, '입니다.')
print('내년에는', age+1, '살이 됩니다!')

# 이름을 입력하세요: 철수
# 나이를 입력하세요: 20
# 안녕하세요, 철수 입니다.
# 내년에는 21 살이 됩니다!
```

---
**출력(Output)**
- `print()` 함수 사용
- 콤마`,`로 여러개 나열 가능 (자동으로 띄어쓰기 한 칸 삽입)

```Python
print('Hello World!') # Hello World!
print('이름:', '철수') # 이름: 철수
```

출력 옵션
- `sep` : 구분자 설정
- `end` : 줄 끝 문자 설정
```Python
print('A', 'B', 'C', sep='-') # A-B-C

print('Hello', end=' ')
print('World')
# Hello World
```

출력 서식 (f-string)
```Python
name = '길동'
age = 20

print(f'안녕하세요, {name}입니다. 나이는 {age}살 입니다.')
# 안녕하세요, 철수입니다. 나이는 20살 입니다.
```

출력 서식(format)
```Python
# 위치 인수
print('이름: {}, 나이: {}'.format('홍길동', 20))

# 인덱스를 사용하여 순서를 명시적으로 지정
print('순서 지정: {1}과 {0}'.format('사과', '바나나'))

# 키워드 인수
print('이름: {name}, 나이: {age}'.format(age=20, name='홍길동'))

# 고정 소수점
print("파이 값 (소수점 2자리): {:.2f}".format(3.141592)) # 3.14

# 쉼표를 사용하여 천 단위 구분
print("큰 숫자 (천 단위 구분): {:,d}".format(1000000)) # 큰 숫자 (천 단위 구분): 1,000,000

# 숫자의 부호 강제 표시 (+ 부호)
print("양수 부호 표시: {:+}".format(10))  # +10
print("음수 부호 표시: {:+}".format(-20)) # -20

# 퍼센트
print("할인율 (퍼센트): {:.1%}".format(0.88)) # 할인율 (퍼센트): 88.0%

# 정렬 및 패딩
text = 'Hello'
print("오른쪽 정렬: '{:>11}'".format(text))  # 오른쪽 정렬: '      Hello'
print("왼쪽 정렬: '{:<11}'".format(text))   # 왼쪽 정렬: 'Hello      '
print("가운데 정렬: '{:^11}'".format(text))  # 가운데 정렬: '   Hello   '
print("패딩 채우기: '{:*^11}'".format(text)) # 패딩 채우기: '***Hello***'
```

---
## 5. 변수(Variables)란?
- 변수는 값을 저장하는 메모리 공간에 붙이는 이름표(Lable)이다.  
- 파이썬에서는 변수에 특정 타입이 고정되지 않고, 언제든지 다른 타입의 객체를 가리킬 수 있다.

**선언 및 할당** : 변수를 선언할 떄 타입을 명시할 필요 없이, 할당 연산자 `=` 를 사용하여 값을 대입한다.
```Python
x = 10         # 정수형 객체를 가리킴
str = 'Hello'  # 문자열 객체를 가리킴
is_True = True # 불리언 객체를 가리킴
```
**동적 타이핑** : 파이썬은 동적 타이핑  언어로, 변수는 프로그램 실행 중에 서로 다른 타입의 객체를 자유롭게 참조할 수 있다.
```Python
data = 100    # int 타입 참조
data = 'text' # str 타입 참조 (같은 이름이 다른 객체를 가리키도록 참조가 변경됨)
```
---
## 5-1. 변수명 규칙
- 변수명은 모두 소문자로 작성 `user_name` `total_count`
- 변수명은 숫자로 시작할 수 없음 `1st_data` (X)
- 두 단어 이상은 언더스코어로 구분 `my_score` `my_string`
- 예약어 사용 불가 `if` `for` `class` `def`
- 값이 절대 변하지 않는 상수는 모두 대문자로 작성 `MAX_VALUE`, `PI`

---
## 6. 함수(Functions)란?
- 함수는 특정 작업을 수행하는 코드 블록을 이름으로 묶은 것
- 코드를 재사용하고 모듈화하는데 사용된다.
- `def` 키워드를 사용하여 함수를 정의한다.
- 0개 이상의 인수를 받을 수 있으며, `return` 키워드를 사용하여 결과를 반환한다.
- `return` 문이 없으면 기본적으로 `None`을 반환한다.

```Python
# 기본 함수
def greet():
    print('안녕하세요!')

greet() # 안녕하세요!
```

---
## 6-1. 함수의 매개변수(Parameters)
- 함수의 매개변수는 함수 정의 시 입력 받을 값을 지정하는 변수이다.
- 호출 시 전달되는 실제 값은 **인수(Argument)**라고 한다.

**위치 인수** : 호출 시 전달되는 인수의 순서에 따라 매개변수에 값이 할당된다.
```Python
def greet(name, message):
    return f'{name}님, {message}'

print(greet('홍길동', '안녕하세요!'))
# 함수 호출 시 순서가 중요하다.
# 홍길동님, 안녕하세요!
```

**키워드 인수** : 호출 시 매개변수 이름을 명시하여 값을 할당한다.
```Python
def greet(name, message):
    return f'{name}님, {message}'

print(greet(message='안녕하세요!', name='홍길동'))
# 키워드 사용으로 순서가 바뀌어도 문제 없음
# 홍길동님, 안녕하세요!
```

**기본 매개변수** : 함수 정의 시 매개변수에 기본값을 지정하여, 호출 시 해당 인수가 전달되지 않으면 기본값이  사용된다. 기본 매개변수는 항상 일반 매개변수 뒤에 위치해야 한다.
```Python
def greet(name, message='안녕하세요!'):
    return f'{name}님, {message}'

print(greet('홍길동'))
print(greet('김길동', '반갑습니다!'))
# 홍길동님, 안녕하세요!
# 김길동님, 반갑습니다!
```

**가변 인자** : 함수가 미리 정해지지 않은 개수의 인수를 받도록 할 때 사용된다.
- `*args` : 위치 인수를 **튜플** 형태로 받는다.
- `**kwargs` : 키워드 인수를 **딕셔너리** 형태로 받는다.
```Python
def my_score(name, *args):
    total = sum(args)
    return f'{name}님의 점수 평균: {total/len(args):.0f}'

print(my_score('홍길동', 100, 80, 70))
# 홍길동님의 점수 평균: 83
```