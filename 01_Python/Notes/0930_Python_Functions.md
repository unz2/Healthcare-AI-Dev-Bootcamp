# Python 함수

## 🎯 목표
1. 함수와 매개변수
2. 람다 함수
3. 조건문
4. 내장 함수
---
## 1. 함수(Functions)란?
- 함수는 특정 작업을 수행하는 코드 블록을 이름으로 묶은 것
- 코드를 재사용하고 모듈화하는데 사용된다.
- `def` 키워드를 사용하여 함수를 정의한다.
- 0개 이상의 인수를 받을 수 있으며, `return` 키워드를 사용하여 결과를 반환한다.
- `return` 문이 없으면 기본적으로 `None`을 반환한다.

```Python
# 함수 정의 문법
def 함수이름(매개변수):
    실행할 코드
    return 결과값
```

---
## 1-1. 함수의 매개변수(Parameters)
- 함수의 매개변수는 함수 정의 시 입력 받을 값을 지정하는 변수이다.
- 호출 시 전달되는 실제 값은 **인수(Argument)**라고 한다.

**매개변수가 없는 함수**
```Python
def greet():
    return '안녕하세요'

print(greet()) # 안녕하세요
```

**매개변수가 있는 함수**
```Python
def add(a, b):
    return a + b

print(add(1, 2)) # 3
```

**위치 인수** : 호출 시 전달되는 인수의 순서에 따라 매개변수에 값이 할당된다.
```Python
def greet(name, message):
    return f'{name}님, {message}'

print(greet('홍길동', '안녕하세요!'))
# 함수 호출 시 순서가 중요하다.
# 홍길동님, 안녕하세요!
```

**키워드 인수** : 호출 시 매개변수 이름을 명시하여 값을 할당한다.
```Python
def greet(name, message):
    return f'{name}님, {message}'

print(greet(message='안녕하세요!', name='홍길동'))
# 키워드 사용으로 순서가 바뀌어도 문제 없음
# 홍길동님, 안녕하세요!
```

**기본 매개변수** : 함수 정의 시 매개변수에 기본값을 지정하여, 호출 시 해당 인수가 전달되지 않으면 기본값이  사용된다. 기본 매개변수는 항상 일반 매개변수 뒤에 위치해야 한다.
```Python
def greet(name, message='안녕하세요!'):
    return f'{name}님, {message}'

print(greet('홍길동'))
print(greet('김길동', '반갑습니다!'))
# 홍길동님, 안녕하세요!
# 김길동님, 반갑습니다!
```

**가변 인자** : 함수가 미리 정해지지 않은 개수의 인수를 받도록 할 때 사용된다.
- `*args` : 위치 인수를 **튜플** 형태로 받는다.
- `**kwargs` : 키워드 인수를 **딕셔너리** 형태로 받는다.
```Python
def my_score(name, *args):
    total = sum(args)
    return f'{name}님의 점수 평균: {total/len(args):.0f}'

print(my_score('홍길동', 100, 80, 70))
# 홍길동님의 점수 평균: 83
```

---
## 1-2. 함수 설명(Docstring)
- 함수에 **설명 문서**를 기입할 수 있음
- 협업, 유지보수에 매우 유용한 기능이다.
```Python
def square(x):
    '''숫자를 제곱해서 반환하는 함수'''
    return x ** 2

help(square)
```

---
## 1-3. 함수와 변수의 범위(Scope)
- 함수 안에서 만든 변수는 밖에서 쓸 수 없다.
```Python
def make_num():
    x = 10
    return x

print(make_num()) # 10
print(x)          # error! x는 함수 안에서만 존재
```

---
## 2. 람다(Lambda) 함수
- 람다 함수는 익명 함수(anonymous function)로, 간단한 함수를 한 줄로 만들 때 사용한다.
- `lambda 매개변수: 표현식` 의 형태로 사용한다.

```Python
# 람다 함수
add_lambda = lambda a, b: a + b

# 일반 함수
def add(a, b):
    return a + b

print(add_lambda(1, 2)) # 3
print(add(1, 2)) # 3
```

---
## 3. 조건문(if, elif, else)
- 주어진 조건에 따라 코드 블록을 실행하는 제어 흐름문이다.
```Python
# boolean 결과(True/False)에 따라 실행
x = 10

if x > 0:
    print('양수')
elif x == 0:
    print('0')
else:
    print('음수')

# 삼항 연산자 (조건을 한 줄로 표현 가능)
age = 20
status = '성인' if age >= 18 else '미성년자'
print(status) # 성인

# 삼항 연산자 응용
score = 88
grade = 'A' if score > 95 else 'B' if score > 85 else 'C' if score > 75 else 'D'
print('등급:', grade) # 등급: B
```

---
## 4. 내장 함수
- 파이썬에서 기본으로 제공하는 함수이다.
- 별도 `import` 없이 바로 사용 가능하다.

### **map**
- `map(function, iterable)`
- Iterable 객체의 모든 요소에 함수를 적용한 결과를 반환한다.
```Python
nums = [1,2,3]
squared = list(map(lambda x: x**2, nums))
print(squared) # [1,4,9]
```

### **filter**
- `filter(function, iterable)`
- Iterable 객체의 요소 중 함수를 적용했을 때 결과가 True인 요소들만 반환한다.
```Python
nums = [1,2,3,4,5]
evens = list(filter(lambda x: x%2==0, nums))
print(evens) # [2,4]
```

### **zip**
- `zip(*iterable)`
- 여러 개의 Iterable 객체를 인수로 받아 각 객체의 같은 인덱스에 해당하는 요소들을 묶어서 튜플 형태로 반환한다.
```Python
fruits = ['apple', 'banana']
colors = ['red', 'yellow']
paired = list(zip(fruits, colors)) 
# [('apple', 'red'), ('banana', 'yellow')]
```

### **len**
- `len(s)`
- 객체(시퀀스나 컬렉션)의 길이나 항목의 개수를 반환한다.
```Python
nums = [1, 2, 3]
print(len(nums)) # 3
```

### **sum**
- `sum(iterable, start=0)`
- Iterable 객체의 모든 요소의 합을 반환한다.
- `start` 인수는 합계에 추가될 초기 값
```Python
nums = [1, 2, 3]
print(sum(nums)) # 6
```

### **max, min**
- `max(iterable)` 반복 가능한 객체의 최댓값을 반환한다.
- `min(iterable)` 반복 가능한 객체의 최솟값을 반환한다.
```Python
nums = [1, 2, 3]
print(max(nums)) # 3
print(min(nums)) # 1
```

### **sorted**
- `sorted(iterable, key=None, reverse=False)`
- 새로운 정렬된 Iterable 객체를 반환한다.
- 원본 객체는 변경되지 않는다.
```Python
words = ['apple', 'banana', 'kiwi']
print(sorted(words))          # ['apple', 'banana', 'kiwi']
print(sorted(words, key=len)) # ['kiwi', 'apple', 'banana']
```

### **any**
- `any(iterable)`
- Iterable 객체의 요소 중 하나라도 True이면 True를 반환한다.
- 모든 요소가 False이거나, 객체가 비어있을 경우에만 False를 반환한다.
```Python
values = [False, False, True]
print(any(values)) # True
```

### **all**
- `all(iterable)`
- Iterable 객체의 모든 요소가 True이면 True를 반환한다.
- 하나라도 False이거나, 객체가 비어있지 않은 경우에만 False를 반환한다.
```Python
values = [True, True, True]
print(any(values)) # True
```