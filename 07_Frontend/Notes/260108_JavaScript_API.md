# JavaScript API

> 🗓️ **2026-01-08**  
> ✍🏼 **작성자 : unz**

## 📝 목차

1. 화살표 함수(Arrow Function)
2. Web API란?
3. Timer API
4. Storage API
5. Fetch API
---

## 1. 화살표 함수(Arrow Function)

> `function` 키워드 대신 화살표(`=>`)를 사용하여 함수를 더 짧고 간결하게 작성하는 방식

- 콜백 함수 문법 단순화
- 불필요한 this 바인딩 문제 해결
- 함수형 프로그래밍 스타일 강화

```js
// 기존 함수 표현식
const add = function (a, b) {
  return a + b;
};

// 화살표 함수
const add = (a, b) => {
  return a + b;
};
```

### 1-1. 문법 축약 규칙

```js
// 1) 매개변수가 하나일 때
// 매개변수 괄호 생략 가능
const square = x => {
  return x * x;
};

// 2) 실행문이 한 줄이고 return만 있을 때
// {} 중괄호, return 생략 가능
const square = x => x * x;

// 3) 매개변수가 없을 때
// 이 경우에는 () 괄호 반드시 필요
const greet = () => "Hello";

// 4) 객체를 바로 반환할 때
// 객체 {} 와 함수블록 {} 구분을 위해 반드시 () 괄호로 감싸야 함
const createUser = (name, age) => ({ name: name, age: age });
```

### 1-2. 화살표 함수와 this

> 화살표 함수는 자신만의 this를 가지지 않는다.  
> 선언 시점의 상위 스코프 this를 그대로 사용

```js
// 일반 함수 this 문제
const person = {
  name: "홍길동",
  greet: function () {
    setTimeout(function () {
      console.log(this.name);
    }, 1000);
  },
};

person.greet(); // undefined

// 화살표 함수로 해결
// 화살표 함수가 상위 스코프를 그대로 참조
const person = {
  name: "홍길동",
  greet: function () {
    setTimeout(() => {
      console.log(this.name);
    }, 1000);
  },
};

person.greet(); // 홍길동
```

## 2. Web API란?

> 브라우저 환경에서 실행될 떄 추가적인 기능을 수행할 수 있게 해주는 기능 집합  
> 자바스크립트 언어 자체에는 없지만, 브라우저 환경에서 사용 가능한 API

- 브라우저 환경(런타임)에서 제공된다.
- 복잡한 기능을 단순한 인터페이스로 조작할 수 있게 한다.
- 비동기적으로 동작하는 경우가 많다.

### 2-1. Web API의 주요 예시

#### DOM API

- HTML 문서의 구조를 객체 모델로 나타내어 자바스크립트가 페이지의 내용, 구조, 스타일을 동적으로 수정할 수 있게 한다.
- `document.getElementById()`, `document.querySelector()`

#### Event API

- 사용자의 클릭, 키보드 입력, 마우스 이동 등 브라우저에서 발생하는 이벤트를 감지하고 반응하게 한다.
- `element.addEventListener`

#### Timer API

- 특정 시가이 지난 후 코드를 실행하거나 일정 간격으로 반복 실행할 때 사용한다.
- `setTimeout`, `setInterval`

#### Storage API

- 브라우저에 데이터를 저장할 수 있는 기능을 제공한다.
- `LocalStorage` : 브라우저를 닫아도 데이터가 유지됨
- `SessionStorage` : 브라우저를 닫으면 데이터가 삭제됨

#### Fetch API

- 네트워크를 통해 서버와 통신하여 데이터를 주고받는 인터페이스

## 3. Timer API

> 브라우저가 제공하는 비동기 실행 매커니즘, 특정 시점에 코드를 실행할 수 있게 해준다.

### 3-1. setTimeout

> 지정된 시간이 지난 후 한 번만 콜백 함수를 실행한다.

```js
// 문법: setTimeout(함수, ms);
setTimeout(() => {
  console.log("3초 후 실행됨");
}, 3000);
```

### 3-2. setInterval

> 지정된 시간 간격마다 콜백 함수를 반복적으로 실행한다.

```js
// 문법: setInterval(함수, ms);
const id = setInterval(() => {
  console.log("1초마다 실행");
}, 1000);
```

### 3-3. timerId

> `setTimeout` / `setInterval이` 타이머를 구분하기 위해 돌려주는 번호  
> 타이머를 멈추거나 버튼 여러 번 눌러도 타이머 하나만 실행되게 하기 위해 사용

```js
// 1초 뒤 실행되는 setTimeout을 즉시 취소하여 실행되지 않음
const timeoutId = setTimeout(() => {
  console.log("실행");
}, 1000);

clearTimeout(timeoutId); // timeoutId 즉시 취소

// 1초마다 count 출력, 5번 출력 후 interval 중지
let count = 0;

const timerId = setInterval(() => {
  count++;
  console.log(`count: ${count}`);

  if (count == 5) {
    clearInterval(timerId);
    console.log("interval 중지");
  }
}, 1000);
```

## 4. Storage API

> 브라우저가 제공하는 key-value 쌍으로 데이터를 저장할 수 있는 저장소 API

- 서버가 아닌 사용자 브라우저에 데이터가 저장된다.
- 문자열(String)만 저장한다.

### 4-1. localStorage

- 브라우저를 닫거나 컴퓨터를 재시작해도 데이터가 영구적으로 보관된다.
- 동일한 도메인 내에서 모든 탭과 창이 데이터를 공유한다.
- 주요 용도 : 자동 로그인 정보, 사용자 설정, 캐싱된 데이터 등

### 4-2. sessionStorage

- 페이지의 세션이 유지되는 동안만 데이터가 보관된다. (텝을 닫으면 데이터가 삭제 됨)
- 데이터를 생성한 해당 탭 내에서만 유효하다.
- 같은 사이트라도 다른 탭이면 별도의 저장소를 가진다.
- 주요 용도 : 일회성 입력 폼 데이터, 페이지 전환 시 유지해야 하는 임시 상태

### 4-3. 주요 메서드

| 메서드              | 설명             | 예시                                         |
| ------------------- | ---------------- | -------------------------------------------- |
| setItem(key, value) | 데이터 저장      | `localStorage.setItem("name", "홍길동");`    |
| getItem(key)        | 데이터 읽기      | `const name = localStorage.getItem("name");` |
| removeItem(key)     | 특정 데이터 삭제 | `localStorage.removeItem("name");`           |
| clear()             | 전체 데이터 삭제 | `localStorage.clear();`                      |
| length              | 저장된 항목 개수 | `localStorage.length;`                       |

### 4-4. 객체 저장하기

- Storage API는 오직 문자열만 저장한다.
- 따라서, 객체나 배열을 저장하려면 `JSON.stringify()`로 직렬화하고, 읽어올 때 `JSON.parse()`로 역직렬화해야한다.

```
JSON(JavaScript Object Notation) 이란?
데이터를 저장하고 전송할 때 사용하는 가장 대중적인 데이터 형식
데이터를 key : value 형식으로 저장한다.
```

```js
const user = { name: "홍길동", age: 20 }; // Object
localStorage.setItem("userKey", JSON.stringify(user)); // 직렬화
JSON.parse(localStorage.getItem("userKey")); // 역직렬화

const numbers = [1, 2, 3]; // 배열
localStorage.setItem("numKey", JSON.stringify(numbers)); // 직렬화
JSON.parse(localStorage.getItem("numKey")); // 역직렬화
```

## 5. Fetch API

> 브라우저가 제공하는 서버 통신용 Web API

- HTTP 요청(Request)를 보내고 응답(Response)를 수신한다.
- 비동기로 동작한다.

```
비동기(Asyncronous)란?
- 오래 걸리는 작업을 기다리지 않고, 다음 코드를 먼저 실행하는 방식
- 서버 요청은 시간이 걸리기 때문에 요청은 보내고, 다른 작업을 계속 진행
- fetch는 서버에 요청을 보내고, 응답이 오면 그때 결과를 처리
```

### 5-1. 주요 메서드

```js
// 기본구조
fetch(url, options)
  .then((response) => response.json())
  .then((data) => console.log(data))
  .catch((error) => console.error(error));

// GET - 데이터를 가져올 때 (기본값)
fetch("https://jsonplaceholder.typicode.com/posts")
  .then((response) => response.json())
  .then((data) => {
    console.log(data);
  });

// POST - 새로운 데이터를 생성할 때
fetch("https://jsonplaceholder.typicode.com/posts", {
  method: "POST",
  headers: { "Content-Type": "application/json" },
  body: JSON.stringify({ title: "hello", body: "world" }),
})
  .then((response) => response.json())
  .then((data) => {
    result.textContent = JSON.stringify(data, null, 2);
  });
```
