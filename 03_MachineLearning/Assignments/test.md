# 웹 서비스 기초와 구조에 대한 이해
> 🗓️ **2025-12-23**  
✍🏼 **작성자 : unz**
---

## 📝 목차
1. 웹(Web)이란?
2. 웹 서비스란?
3. 인터넷 상 웹이 아닌 다른 통신 방식
4. 초기 인터넷 통신
5. 클라이언트 - 서버 모델의 등장
6. HTTP란?
7. 클라이언트의 HTTP 요청 구조
8. REST API
9. Query Parameter
10. 데이터를 서버로 전송하는 방법
---

## 1. 웹(Web)이란?
- 인터넷 위에서 표준 규약으로 자원과 정보를 제공하는 시스템

|||
|:--:|--|
|인터넷 위에서| 전 세계 컴퓨터들이 연경된 네트워크를 사용한다.|
|표준 규약으로|모두가 약속한 공통 규칙을 사용한다. (URL, HTTP, HTTPS)|
|자원과 정보를| 웹페이지, 이미지, 영상, 데이터, API 등|
|제공하는| 요청하면 서버가 응답|
|시스템| 사람 ∙ 프로그램이 함께 작동하는 구조|

## 2. 웹 서비스란?
- 웹(Web)을 통해 제공되는 서비스
    - 검색 - Google, Naver
    - SNS - Instagram, X(트위터), Facebook
    - 메신저 - WhatsApp, 카카오톡
    - 전자상거래 - Amazon, 쿠팡
    - 스트리밍 - YouTube, Netflix
    - SaaS - Notion, Figma, Google Docs
    - API 중심 서비스 - 결제 API, 지도 API, 로그인 API

### 웹 서비스의 특징
- 인터넷을 기반으로 동작한다
- 여러 사용자가 동시에 이용할 수 있다
- 플랫폼과 기기에 독립적이다
- 브라우저 또는 설치형 클라이언트를 통해 접근한다
    - 데스크톱 앱(macOS, Windows), 모바일 앱 (iOS, Android)

## 3. 인터넷 상 웹이 아닌 다른 통신 방식
- **이메일 서비스** : SMTP를 사용하는 메시지 전달 방식
- **파일 전송 서비스** : FTP / SFTP를 사용하는 파일 전송 방식
- **온라인 게임** : UDP 기반의 저지연 실시간 통신 방식
- **실시간 음성·영상 통신** : WebRTC / RTP 기반의 실시간 통신 방식

## 4. 초기 인터넷 통신
초기 인터넷 통신
- 탄생 배경 : 전쟁 상황에서도 끊기지 않는 통신을 유지하기 위해 1969년 미국 국방부 ARPANET 구축
- 목적 : 원거리 컴퓨터 간의 연결과 데이터 전달
- 한계점 : 여러 컴퓨터가 같은 데이터를 원함 → 데이터를 중앙에서 관리할 필요 발생 → 항상 응답해 줄 고정된 대상 필요
- 해결책 : 데이터를 한 곳에 모아 항상 켜져 있으면서, 요청이 오면 응답해주는 역할을 만든다.
- 역할의 분리
    - 데이터와 기능을 제공하는 쪽 → 서버(Server)
    - 필요할 때 요청하는 쪽 → 클라이언트(Client)

## 5. 클라이언트 - 서버 모델의 등장
- 역할이 명확히 분리된다.
- 요청과 응답의 방향이 정해져 있다. (클라이언트 → 서버)
- 여러 클라이언트가 하나의 서버를 사용할 수 있다.
- 서버는 항상 대기하며 요청을 처리한다.
- 서버에서 데이터와 기능을 중앙에서 관리한다.
- 역할은 통신 상황에 따라 달라질 수 있다.
    - 브라우저 → 웹 서버 : 브라우저(클라이언트), 웹 서버(서버)
    - 웹 서버 → 데이터베이스 : 웹 서버(클라이언트), 데이터베이스(서버)
```
Client → Request → Server
Client ← Response ← Server
```

### 5-1. 클라이언트와 서버는 어떻게 서로 이해할까?
```
예시)
클라이언트 : "내가 주문한 내역 조회해줘"
    서버 : "뭘? 어디서? 어떤 형식으로?"

문제상황)
    프로그램끼니는 어떤 언어로 소통해야 할까?
    클라이언트와 서버가 서로 다른 프로그래밍 언어로 만들어져 있다면?
    클라이언트와 서버가 서로 다른 운영체제에서 실행되고 있다면?

→ 공통된 통신 규칙이 필요하다. HTTP 발명
```

## 6. HTTP란?
- HyperText Transfer Protocol
- 클라이언트와 서버가 요청과 응답 메시지를 어떻게 주고 받을지 정한 통신 규칙

### 6-1. HTTP의 구성
- 이 메시지는 요청인지, 응답인지
- 무엇을 요청하는지 (메서드)
- 요청이 성공했는지 실패했는지 (상태 코드)
- 데이터는 어떤 형식인지 (헤더, 본문)

### 6-2. HTTP 요청 보내기
`curl` : HTTP 요청을 보내기 위한 커맨드라인 도구

```bash
$ curl -I example.com
$ curl -I example.com -v
```

### 6-2. HTTP 메시지 종류 및 구성
**종류** : 요청 메시지, 응답 메시지  
**구성**
```
시작 줄(Start Line)
    요청 : 메서드 + URL + HTTP 버전
    응답: 상태 코드 + 메시지
헤더(Header)
    메타데이터(형식, 길이, 인증 정보 등)
본문(Body)
    실제 전달되는 데이터(optional)
```

```
요청 메시지(Request Message)
HEAD / HTTP/1.1
Host: example.com
User-Agent: curl/8.7.1
Accept: */*

응답 메시지(Response Message)
HTTP/1.1 200 OK
Content-Type: text/html
Connection: keep-alive
…
```

### 6-3. 웹 서비스와 HTTP 동작원리
1. 사용자는 UI를 조작한다.
2. 클라이언트 프로그램은 사용자의 행동을 의도에 맞는 요청으로 해석한다.
3. 클라이언트는 해당 요청을 HTTP 요청 메시지로 만들어 서버에 전송한다.
4. 서버는 요청을 처리하고 HTTP 응답 메시지를 반환한다.
5. 클라이언트는 응답을 받아 화면이나 기능으로 반영한다.

## 7. 클라이언트의 HTTP 요청 구조

### 클라이언트의 의도는 `URL + HTTP Method` 로 표현한다.
> 예시)  
GET https://example.com/users → 클라이언트는 전체 사용자 목록 조회를 원한다.   
POST https://example.com/posts/10/comments → 클라이언트는 10번 게시글에 댓글 작성을 원한다.  
DELETE https://example.com/orders/123 → 클라이언트는 123번 주문을 삭제하기를 원한다  

### URL(Uniform Resource Locator) : 웹에서 자원의 위치를 식별하는 주소
> 예시)  
http://example.com/users  → 사용자 목록 자원  
http://example.com/users/1  →  사용자 ID가 1인 자원   
http://example.com/posts/10/comments  → 게시글 10번의 댓글 자원  
http://example.com/files/report.pdf  → 파일 자원

### HTTP Method : 클라이언트가 서버에 어떤 동작을 원하는지 표현하는 방법
> 예시)  
**GET** : 자원 조회 (Read)  
**POST** : 자원 생성 (Create)  
**PUT** : 자원 전체 수정 (Update)  
**PATCH** : 자원 일부 수정 (Update)  
**DELETE** : 자원 삭제 (Delete)  

CRUD : 자원에 대한 대표적인 요청의 개념적 분류  
HTTP Method : 이를 실제 HTTP 요청으로 표현하는 수단

## 7-1. HTTP 요청 시 주의사항
### 1. 같은 자원이라도 메서드에 따라 전혀 다른 의미의 요청이 된다.
```
GET  /users → 사용자 목록 조회
POST /users → 사용자 생성
```

### 2. 행위는 URL이 아니라 메서드로 표현한다.
```
새로운 사용자 추가
/createUser ❌
POST /users ✅

주문 삭제
/deleteOrder ❌
DELETE /orders/123 ✅
```

### 3. 자원은 보통 복수형 명사로 표햔한다.
```
자원은 집합을 나타내는 경우가 많기 때문에 복수형 명사로 표햔한다.
개별 자원은 보통 ID로 구분한다.

GET /users → 사용자 목록(집합)
GET /users/1 → 사용자 한 명
```

### 4. 기본 CRUD를 벗어나는 경우도 있다
```
모든 요청이 생성·조회·수정·삭제로 깔끔하게 나뉘지 않는다.
의미 있는 행위(Action) 자체가 목적이 되는 요청이 존재한다.
이런 경우는 관례적으로 POST를 사용하는 경우가 많다.

POST /orders/123/cancel	→ 주문 취소
POST /auth/login        → 로그인
POST /payments/confirm 	→ 결제 확정
```

## 8. REST API
- Representational State Transfer API
- 웹의 기본 원칙을 따르는 API 설계 방식
- 자원을 중심으로 HTTP Method를 사용해 클라이언트와 서버가 통신하는 API tjfRp

```
Restful 하지 않은 API 설계 예시
GET /getUsers
GET /user-123
POST /users/create
POST /deleteOrders
```

## 9. Query Parameter
- URL에 붙여서 전달하는 데이터
- 주로 조회 조건이나 옵션을 표현할 때 사용한다.
- 경로(path) 뒤에 `?`로 시작, `key=value` 형태, `&` 로 여러개 연결
- 데이터를 어떻게 보고 싶은지 서버에 알려주는 역할

```
예시)
https://www.google.com/search?q=python  → 구글에 파이썬 검색
GET /users?name=kim                     → 이름이 kim인 유저만 조회
GET /products?category=fruit&sort=price → 과일 카테고리 상품을 가격순으로 조회
GET /users?page=2&limit=10              → 2페이지의 유저를 10명씩 조회
```

## 10. 데이터를 서버로 전송하는 방법
- (회원가입) 사용자 정보를 저장해야 할 때
- (글 작성) 새 리소스를 생성할 때
- (프로필 수정) 기존 데이터를 변경할 때
- (로그인) 서버에 판단할 정보를 전달해야할 때

### Request Body
- 클라이언트가 서버로 데이터를 보낼 때, URL에 담기 어려운 크고 복잡한 데이터를 전송하기 위해 사용하는 공간
```
POST /users/login HTTP/1.1
Host: example.com
Content-Type: application/json
Content-Length: 52
{
  "email": "alex@example.com",
  "password": "1234"
}

$ curl -X POST https://httpbin.org/post \
  -H "Content-Type: application/json" \
  -d '{
    "email": "test@example.com",
    "password": "1234"
  }'

```
